# คัดลอกบรรทัดด้านล่างไปวางใน .env (เพิ่มหรือแทนที่ของเดิม)

# ----- Server & DB (แก้แล้วเชื่อม MySQL ได้) -----
SERVER_PORT=500
DB_HOST=localhost
DB_PORT=3308
DB_USER=trackspend_user
DB_PASSWORD=trackspend_pass
DB_NAME=trackspend
JWT_SECRET_KEY=your_very_secret_key_change_this_in_production

# ----- CORS (ถ้าต้องการจำกัด origin) -----
# ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
