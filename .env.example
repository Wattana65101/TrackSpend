# Copy to .env and fill in values. See .env for required keys.

# Optional: editor when tapping file path in React Native error (default on Windows = Notepad)
# REACT_EDITOR=cursor
# Or full path: REACT_EDITOR=C:\Users\admin\AppData\Local\Programs\cursor\Cursor.exe
