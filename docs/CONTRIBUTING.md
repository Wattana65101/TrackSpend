# Contributing to TrackSpend

ขอบคุณที่สนใจจะ contribute ให้กับ TrackSpend! 🎉

## การเริ่มต้น

1. Fork repository นี้
2. Clone repository ที่ fork มา:
   ```bash
   git clone https://github.com/YOUR_USERNAME/TrackSpend.git
   cd TrackSpend
   ```
3. สร้าง branch ใหม่สำหรับ feature ของคุณ:
   ```bash
   git checkout -b feature/your-feature-name
   ```

## การพัฒนา

1. ติดตั้ง dependencies:
   ```bash
   npm install
   ```

2. ตั้งค่า environment variables:
   - คัดลอก `.env.example` เป็น `.env`
   - แก้ไขค่าต่างๆ ให้เหมาะสม

3. รันแอป:
   ```bash
   npm start
   npm run android  # หรือ npm run ios
   ```

## Guidelines

### Code Style
- ใช้ ESLint และ Prettier ที่มีอยู่แล้ว
- เขียนโค้ดให้อ่านง่ายและมี comments ที่จำเป็น
- ใช้ชื่อตัวแปรและฟังก์ชันที่สื่อความหมาย

### Commit Messages
- ใช้ภาษาไทยหรืออังกฤษก็ได้
- เขียน commit message ให้ชัดเจน
- ตัวอย่าง: `feat: เพิ่มฟีเจอร์ export รายงาน`, `fix: แก้ไข bug การแสดง username`

### Pull Requests
- เขียน description ให้ชัดเจนว่าทำอะไร
- อ้างอิง issue ที่เกี่ยวข้อง (ถ้ามี)
- ทดสอบให้แน่ใจว่าโค้ดทำงานได้ถูกต้อง
- รอให้ maintainer review ก่อน merge

## การรายงาน Bug

- ใช้ GitHub Issues
- อธิบายปัญหาให้ชัดเจน
- แนบ screenshots (ถ้ามี)
- บอก environment ที่ใช้ (OS, React Native version, etc.)

## การเสนอ Feature

- เปิด issue เพื่ออธิบาย feature ที่ต้องการเพิ่ม
- รอ feedback จาก maintainer ก่อนเริ่มเขียนโค้ด

## คำถาม

ถ้ามีคำถามสามารถเปิด issue หรือติดต่อ maintainer ได้เลย

ขอบคุณที่ช่วยพัฒนา TrackSpend! 🙏

